# 🧾 오답노트 — 할인 행사 (프로그래머스 Lv.2)

### 📅 풀이 일자

2025-11-06

---

### 📌 문제 요약

정현이는 원하는 제품(`want`)과 각각 필요한 개수(`number`)를 정해두었다.
마트에서는 매일 하나의 제품이 할인되고(`discount`),
**10일 연속 할인 품목이 원하는 제품 구성과 정확히 일치할 경우**
회원가입을 하여 구매한다.

> 가능한 회원가입 날짜(=10일 연속 할인 구간) 개수를 구하라.

---

### 🧩 입출력 예시

```js
const want = ["banana", "apple", "rice", "pork", "pot"];
const number = [3, 2, 2, 2, 1];
const discount = [
  "chicken",
  "apple",
  "apple",
  "banana",
  "rice",
  "apple",
  "pork",
  "banana",
  "pork",
  "rice",
  "pot",
  "banana",
  "apple",
  "banana",
];

solution(want, number, discount); // ➜ 3
```

---

### 💀 나의 초기 코드 (오답 원인)

```js
function solution(want, number, discount) {
  let answer = 0;
  let numArr = new Array(number.length).fill(0);

  for (let i = 0; i < discount.length; i++) {
    for (let k = 0; k < want.length; k++) {
      // ...
    }
  }

  return answer;
}
```

#### ❌ 문제점 정리

| 구분                               | 설명                                 | 영향                     |
| ---------------------------------- | ------------------------------------ | ------------------------ |
| **10일 구간 고려 안 함**           | 단순히 전체 `discount` 순회만 함     | 10일 연속 구간 판단 불가 |
| **numArr 누적만 하고 초기화 없음** | 10일마다 새로 세야 하는데 계속 누적  | 모든 구간이 왜곡됨       |
| **비교 로직 없음**                 | `numArr`와 `number` 비교 과정이 누락 | 조건 판정 자체 불가      |

---

### ✅ 올바른 접근 (내 사고방식 유지형)

```js
function solution(want, number, discount) {
  let answer = 0;
  const idx = new Map();
  for (let i = 0; i < want.length; i++) idx.set(want[i], i);

  for (let start = 0; start <= discount.length - 10; start++) {
    const numArr = new Array(number.length).fill(0);

    for (let d = start; d < start + 10; d++) {
      const item = discount[d];
      if (idx.has(item)) numArr[idx.get(item)]++;
    }

    let ok = true;
    for (let i = 0; i < number.length; i++) {
      if (numArr[i] !== number[i]) {
        ok = false;
        break;
      }
    }
    if (ok) answer++;
  }

  return answer;
}
```

---

### 🔍 핵심 개념 정리

| 개념                      | 설명                                          | 적용 포인트                                  |
| ------------------------- | --------------------------------------------- | -------------------------------------------- |
| **슬라이딩 윈도우**       | 연속된 범위(여기선 10일)를 하나씩 옮기며 검사 | `for(start = 0; start <= len - 10; start++)` |
| **카운팅 배열(`numArr`)** | 각 품목별 할인 개수를 인덱스 기반으로 관리    | `want` 인덱스와 `Map`으로 매칭               |
| **비교 기준**             | `numArr[i] === number[i]` 인지 전부 확인      | 완벽 일치 시 회원가입 가능                   |
| **시간 복잡도**           | O(n×10) = O(n) 수준                           | 충분히 통과 가능                             |

---

### 🚀 개선 버전 (슬라이딩 갱신)

```js
function solution(want, number, discount) {
  let answer = 0;
  const idx = new Map();
  for (let i = 0; i < want.length; i++) idx.set(want[i], i);

  if (discount.length < 10) return 0;

  const numArr = new Array(number.length).fill(0);
  for (let d = 0; d < 10; d++) {
    if (idx.has(discount[d])) numArr[idx.get(discount[d])]++;
  }

  const same = () => number.every((n, i) => n === numArr[i]);
  if (same()) answer++;

  for (let start = 1; start <= discount.length - 10; start++) {
    const outItem = discount[start - 1];
    const inItem = discount[start + 9];
    if (idx.has(outItem)) numArr[idx.get(outItem)]--;
    if (idx.has(inItem)) numArr[idx.get(inItem)]++;
    if (same()) answer++;
  }

  return answer;
}
```

---

### ⚖️ 비교 요약

| 버전                           | 장점            | 단점                | 추천도           |
| ------------------------------ | --------------- | ------------------- | ---------------- |
| **재계산형** (`numArr` 초기화) | 단순, 실수 적음 | 매번 10개 새로 셈   | 🟢 초보자 안정형 |
| **갱신형** (슬라이딩)          | 빠름, 효율적    | 구현 실수 여지 있음 | 🔵 실전용 추천   |

---

### 🧠 배운 점

- 인덱스 관리(`i`, `k`)는 실수 한 번으로 전체 로직이 깨짐 → **반복문 변수 이름 정확히 관리**
- 배열 기반 카운트는 **`Map`보다 빠르지만**, 반드시 인덱스 매칭을 정확히 해야 함
- 슬라이딩 윈도우 문제는 항상
  “**초기 세팅 → 빠지는 요소 → 새로 들어오는 요소 → 비교 함수**” 구조로 생각하면 깔끔하게 정리됨

---

### 🧩 사고 전환 포인트

- ✅ **이진트리 X → 일반 트리 O**
- ✅ **DP X → 탐색 기반 접근 O**
- ✅ **끊은 후 탐색**이 핵심 (끊기 전엔 의미 없음)
- ✅ 각 노드는 직접 연결된 노드만 저장해야 함 (직접 연결 관계만)

---

### 🧮 복잡도 분석

- 전선 n-1개 중 하나씩 끊고 DFS 수행 → O(n²) 이하
- n 최대 100 → 완전탐색 가능 (효율성 문제 없음)

---

### 💬 깨달음 / 다음에 주의할 점

- 무방향 트리에서는 “부모-자식” 관계가 없다는 점을 항상 인지하자.
- 그래프 문제는 언제나 **인접 리스트 + 탐색으로 연결 확인**이 기본이다.
- DP는 ‘방향성과 부분 문제 재활용’이 있을 때만 쓸 수 있다.

---

### 🔁 다음 복습 포인트

- [ ] 그래프 인접 리스트 직접 구성 연습하기
- [ ] DFS/BFS 탐색으로 연결된 노드 수 세기 구현 연습
- [ ] 트리 구조 문제에서 DP가 가능한 조건 구분하기
